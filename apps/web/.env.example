# Copy to .env.local and fill in. .env.local is gitignored.
# For production (Vercel): paste these in Project → Settings → Environment Variables.

# Resend (contact form)
# Get API key: https://resend.com/api-keys
# "From" must be a verified domain in Resend (or use onboarding@resend.dev for testing)
RESEND_API_KEY=
RESEND_FROM=Portfolio <onboarding@resend.dev>
CONTACT_TO_EMAIL=

# Vercel injects these automatically in production (no need to set locally):
# VERCEL=1
# VERCEL_URL=
# VERCEL_ENV=
