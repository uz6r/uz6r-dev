# Copy to .env.local and fill in. .env.local is gitignored.
# For production (Vercel): paste these in Project → Settings → Environment Variables.

# Resend (contact form)
# API key: https://resend.com/api-keys
RESEND_API_KEY=
# From address. Must be "email@domain" or "Name <email@domain>". No placeholders.
# Testing: use exactly onboarding@resend.dev (e.g. "Portfolio <onboarding@resend.dev>").
# Production: use an address at your verified domain (https://resend.com/domains).
RESEND_FROM=Portfolio <onboarding@resend.dev>
CONTACT_TO_EMAIL=

# Vercel injects these automatically in production (no need to set locally):
# VERCEL=1
# VERCEL_URL=
# VERCEL_ENV=
