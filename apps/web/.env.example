# Copy to .env.local and fill in. .env.local is gitignored.
# No required vars for the portfolio right now.

# Optional: when you add an API or backend, use NEXT_PUBLIC_ only for client-visible values.
# NEXT_PUBLIC_API_URL=https://api.example.com

# Vercel injects these automatically in production (no need to set locally):
# VERCEL=1
# VERCEL_URL=
# VERCEL_ENV=
