# fly.io config intentionally minimal.
# scaling and regions can be added later.

app = "uz6r-api"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
